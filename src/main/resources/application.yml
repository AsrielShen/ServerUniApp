#服务器端口号
server:
  port: 8080

#数据库地址
datasource:
  url: localhost:3306/ai_class


spring:
  application:
    name: ServerUniApp
  #数据库配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${datasource.url}?useSSL=false&useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true&autoReconnect=true&failOverReadOnly=false&maxReconnects=10&serverTimezone=Asia/Shanghai
    username: root
    password: 030326
    hikari:
      maximum-pool-size: 10 # 最大连接池数
      max-lifetime: 1770000

mybatis:
  # 指定别名设置的包为所有entity
  type-aliases-package: com.example.ServerUniApp.entity
  configuration:
    map-underscore-to-camel-case: true # 驼峰命名规范
  mapper-locations: # mapper映射文件位置
    - classpath:mapper/*.xml

#Token 私钥
jwt:
  secret: V8sq1R8Ck+TunIqrTiCby3MPeDJtObZH/jm38pT2XUc=
  expiration: 86400000  #24 * 60 * 60 * 1000 毫秒




